export interface CloudinaryAsset {
    publicId: string;
}

export interface CloudinaryImageProps {
    publicId: string;
    alt?: string;
    className?: string;
}

export interface CloudinaryVideoProps {
    publicId: string;
    className?: string;
}

export interface CloudinaryAudioProps {
    publicId: string;
}